# 이미지 애니메이션 방식 변경 가이드 (상세 버전)

## 📋 개요
이 문서는 웨딩 프로젝트의 애니메이션 시스템을 src 변경 방식에서 visibility 토글 방식으로 통합하는 과정과 중앙정렬 문제 해결 방법을 단계별로 상세히 정리한 가이드입니다.

## 🎯 목표
- 두 가지 애니메이션 방식을 하나의 시스템으로 통합
- 완벽한 중앙정렬 달성
- 기존 동작 100% 유지
- 코드 일관성 및 유지보수성 향상

---

## 1. 📊 애니메이션 방식 상세 분석

### 1.1 Src 변경 방식 (Rabbit Method)

#### 🔍 동작 원리
```html
<!-- HTML 구조 -->
<div class="pixel-character">
    <img src="frame1.png" id="single-img">
</div>
```

```javascript
// JavaScript 동작
function updateFrame(character, frameIndex) {
    const img = character.element.querySelector('img');
    img.src = `${character.framePrefix}${frameIndex + 1}.png`;
}
```

#### ✅ 장점
- **메모리 효율성**: IMG 요소 1개만 사용
- **단순한 DOM 구조**: 적은 HTML 요소
- **빠른 초기 로딩**: 첫 프레임만 로드

#### ❌ 단점
- **깜빡임 현상**: 이미지 로딩 지연으로 인한 플리커
- **네트워크 의존성**: 프레임마다 HTTP 요청
- **캐시 미스**: 브라우저 캐시 되지 않은 이미지 로딩 지연

#### 🎯 적합한 경우
- 프레임 수가 많은 애니메이션 (10+ 프레임)
- 메모리 제약이 있는 환경
- 네트워크가 안정적인 환경

### 1.2 Visibility 토글 방식 (Slime Method)

#### 🔍 동작 원리
```html
<!-- HTML 구조 -->
<div class="pixel-character">
    <img src="frame1.png" style="visibility: visible;">
    <img src="frame2.png" style="visibility: hidden;">
    <img src="frame3.png" style="visibility: hidden;">
    <!-- 모든 프레임이 미리 로드됨 -->
</div>
```

```javascript
// JavaScript 동작
function updateFrame(character, frameIndex) {
    character.frameImages.forEach((img, index) => {
        if (index === frameIndex) {
            img.style.visibility = 'visible';
            img.style.opacity = '1';
        } else {
            img.style.visibility = 'hidden';
            img.style.opacity = '0';
        }
    });
}
```

#### ✅ 장점
- **부드러운 전환**: 모든 프레임이 미리 로드되어 깜빡임 없음
- **즉시 응답**: 프레임 전환이 CSS만으로 처리
- **시각적 품질**: 더 나은 사용자 경험

#### ❌ 단점
- **메모리 사용량 증가**: 프레임 수 × IMG 요소 수
- **복잡한 DOM**: 많은 HTML 요소
- **초기 로딩 시간**: 모든 프레임을 미리 로드

#### 🎯 적합한 경우
- 짧은 애니메이션 (5-10 프레임)
- 부드러운 전환이 중요한 경우
- 메모리 여유가 있는 환경

---

## 2. 🔧 통합 시스템 구현 과정

### 2.1 Phase 1: useVisibilityMethod 플래그 시스템 설계

#### 🎯 목표
기존 시스템을 건드리지 않고 새로운 방식을 옵션으로 추가

#### 📝 구현 코드
```javascript
// addCharacter 함수에서 플래그 처리
addCharacter(name, options) {
    const character = {
        // 기존 속성들...
        useVisibilityMethod: options.useVisibilityMethod || false,  // 새 플래그
        frameImages: [],  // visibility 방식용 배열
    };

    // 방식에 따른 분기 처리
    if (character.useVisibilityMethod && character.isPngSequence) {
        this.createVisibilityFrames(character);  // 새 방식
    } else {
        this.createSrcFrame(character);  // 기존 방식
    }
}
```

#### ⚠️ 실패 사례와 해결
**문제**: 플래그 확인 순서 실수
```javascript
// ❌ 잘못된 순서 (isPngSequence 체크 누락)
if (character.useVisibilityMethod) {
    // isPngSequence가 아닌 경우에도 실행됨
}

// ✅ 올바른 순서
if (character.useVisibilityMethod && character.isPngSequence) {
    // PNG 시퀀스이면서 visibility 방식인 경우만
}
```

### 2.2 Phase 2: Visibility 방식 IMG 생성 로직

#### 📝 상세 구현
```javascript
createVisibilityFrames(character) {
    character.frameImages = [];

    for (let i = 1; i <= character.frameCount; i++) {
        const img = document.createElement('img');
        const framePath = `${character.framePrefix}${i}.png`;

        // 🚨 중요: 초기 스타일 설정
        img.style.cssText = `
            display: block;
            visibility: hidden;
            opacity: 0;
            image-rendering: pixelated;
            image-rendering: -moz-crisp-edges;
            image-rendering: crisp-edges;
            -webkit-user-drag: none;
            -webkit-user-select: none;
            user-select: none;
        `;

        img.src = framePath;
        img.alt = `${character.name} frame ${i}`;

        character.element.appendChild(img);
        character.frameImages.push(img);
    }
}
```

#### 🚨 핵심 주의사항
```javascript
// ❌ 절대 이런 스타일을 넣으면 안 됨!
img.style.cssText = `
    position: absolute;  // 중앙정렬 깨짐의 주범!
    left: 0;            // 부모 transform과 충돌
    top: 0;             // 부모 transform과 충돌
    // 기타...
`;
```

### 2.3 Phase 3: updateFrame 함수 통합

#### 📝 이전 버전 (분리된 함수들)
```javascript
// 기존: 각각 다른 함수
updateFrameRabbit(character) { /* src 방식 */ }
updateFrameSlime(character) { /* visibility 방식 */ }
```

#### 📝 통합 버전
```javascript
updateFrame(character) {
    if (!character.isPngSequence) return;

    if (character.useVisibilityMethod) {
        // Visibility 방식 처리
        if (!character.frameImages || character.frameImages.length === 0) {
            console.error(`frameImages not found for ${character.name}`);
            return;
        }

        character.frameImages.forEach((img, index) => {
            if (index === character.currentFrame) {
                img.style.visibility = 'visible';
                img.style.opacity = '1';
            } else {
                img.style.visibility = 'hidden';
                img.style.opacity = '0';
            }
        });
    } else {
        // Src 방식 처리 (기존)
        if (!character.img) {
            character.img = character.element.querySelector('img');
        }

        const framePath = `${character.framePrefix}${character.currentFrame + 1}.png`;
        character.img.src = framePath;
    }
}
```

#### ⚠️ 실패 사례와 디버깅
```javascript
// 🚨 자주 발생하는 오류들

// 1. frameImages 배열이 비어있는 경우
if (character.frameImages.length === 0) {
    console.error('frameImages is empty!'); // 디버깅용
}

// 2. currentFrame이 범위를 벗어나는 경우
if (character.currentFrame >= character.frameImages.length) {
    console.error(`currentFrame ${character.currentFrame} exceeds frameImages length`);
}

// 3. IMG 요소가 DOM에 없는 경우
character.frameImages.forEach((img, index) => {
    if (!img.parentElement) {
        console.error(`Frame ${index} not in DOM`);
    }
});
```

---

## 3. 🎨 중앙정렬 문제 심층 분석

### 3.1 문제 발생 원인 분석

#### 🔍 HTML 구조 비교
```html
<!-- ✅ Src 방식 (정상 작동) -->
<div class="pixel-character"
     style="left: 50%; top: 60%; transform: translate(-50%, -50%) scale(2);">
    <img src="groom/animation/information-idle/information-idle5.png">
</div>

<!-- ❌ Visibility 방식 (중앙정렬 깨짐) -->
<div class="pixel-character"
     style="left: 50%; top: 60%; transform: translate(-50%, -50%) scale(2);">
    <img src="bride/animation/information-idle/information-idle1.png"
         style="position: absolute; left: 0px; top: 0px; visibility: hidden;">
    <img src="bride/animation/information-idle/information-idle2.png"
         style="position: absolute; left: 0px; top: 0px; display: none;">
    <!-- 계속... -->
</div>
```

#### 🧮 CSS Transform 동작 원리
```css
/* 부모 div의 중앙정렬 CSS */
.pixel-character {
    position: absolute;
    left: 50%;                    /* 부모 기준 50% 지점에 요소의 왼쪽 끝을 배치 */
    top: 60%;                     /* 부모 기준 60% 지점에 요소의 위쪽 끝을 배치 */
    transform: translate(-50%, -50%);  /* 자신의 크기의 50%만큼 왼쪽, 위쪽으로 이동 */
}

/* ❌ 문제가 되는 내부 IMG 스타일 */
img {
    position: absolute;           /* 부모의 transform 계산에 영향을 줌 */
    left: 0;                     /* 부모의 transform과 충돌 */
    top: 0;                      /* 부모의 transform과 충돌 */
}
```

### 3.2 1차 시도: 픽셀 계산 방식 (실패)

#### 📝 시도한 방법
```javascript
// 각 이미지의 실제 크기 측정
const imageSizes = {
    'rabbit-idle': { width: 36, height: 36, scale: 2 },
    'rabbit-hurt': { width: 72, height: 36, scale: 4 },
    'wreath': { width: 144, height: 76, scale: 2 },
    'information-idle': { width: 400, height: 384, scale: 2 }
};

// 보정값 계산
function calculateOffset(imageName) {
    const size = imageSizes[imageName];
    const scaledWidth = size.width * size.scale;
    const halfWidth = scaledWidth / 2;
    return `calc(50% - ${halfWidth}px)`;
}

// 적용
rabbitChar.element.style.left = 'calc(50% - 72px)';   // 36 * 2 = 72
wreathChar.element.style.left = 'calc(50% - 144px)';  // 144 * 1 = 144
```

#### ❌ 실패 이유
```javascript
// 🚨 이 방식의 문제점들

// 1. 컨테이너 크기 변동성
// - 반응형 디자인에서 컨테이너 크기가 달라짐
// - 고정 픽셀값은 모든 상황에 대응 불가

// 2. 스케일 요소의 복잡성
// - CSS transform scale과 이미지 원본 크기의 상호작용
// - 여러 스케일이 중첩되면 계산 복잡

// 3. 이미지 내용의 위치
// - 캔버스 크기와 실제 그려진 내용의 위치가 다름
// - 픽셀 단위 보정으로는 정확한 맞춤 불가능

// 4. 유지보수성
// - 새 이미지 추가 시마다 계산 필요
// - 실수 발생 가능성 높음
```

### 3.3 최종 해결: 근본 원인 제거 (성공)

#### 🎯 핵심 아이디어
"픽셀 보정하지 말고, 충돌하는 스타일을 제거하자"

#### 📝 Step 1: 문제 스타일 제거
```javascript
// 🔍 문제가 되는 코드 위치 파악
// File: simplePixelCharacter.js
// Line: 1574-1584 (addCharacter 함수 내부)

// ❌ 기존 (문제 있던 코드)
img.style.cssText = `
    position: absolute;    // 👈 이것이 주범!
    left: 0;              // 👈 부모 transform과 충돌
    top: 0;               // 👈 부모 transform과 충돌
    display: block;
    visibility: hidden;
    opacity: 0;
    // 기타 스타일들...
`;

// ✅ 수정 후
img.style.cssText = `
    display: block;        // 유지
    visibility: hidden;    // 유지
    opacity: 0;           // 유지
    image-rendering: pixelated;  // 유지
    // position, left, top 완전 제거!
`;
```

#### 📝 Step 2: 부모 요소 positioning 명시
```javascript
// 각 애니메이션의 show 함수에서 부모 div 위치 명시

// ✅ Information-idle
showInformationIdle() {
    const char = this.characters.get('information-idle');
    if (char && char.element) {
        char.element.style.opacity = '1';
        char.element.style.top = '60%';    // 명시적 설정
        char.element.style.left = '50%';   // 명시적 설정
        char.isActive = true;
        this.startAnimation(char);
    }
}

// ✅ Wreath 애니메이션들
startWreathAnimation() {
    const char = this.characters.get('wreath');
    char.element.style.left = '50%';  // calc() 제거하고 단순화
    // 부모 transform: translate(-50%, -50%)가 중앙정렬 담당
}

onWreathAnimationComplete() {
    const char = this.characters.get('wreath-idle');
    char.element.style.left = '50%';  // calc() 제거하고 단순화
}

// ✅ Rabbit 애니메이션들
showRabbitCharacter() {
    const char = this.characters.get('rabbit-idle');
    char.element.style.left = '50%';  // calc(50% - 72px) → 50%
}

updateRabbitPosition() {
    const char = this.characters.get('rabbit-idle');
    char.element.style.left = '50%';  // calc(50% - 72px) → 50%
}

switchRabbitToHurt() {
    const char = this.characters.get('rabbit-hurt');
    char.element.style.left = '50%';  // calc(50% - 144px) → 50%
}
```

#### 🎯 결과적 HTML 구조
```html
<!-- ✅ 최종 성공한 구조 -->
<div class="pixel-character"
     style="position: absolute; left: 50%; top: 60%; transform: translate(-50%, -50%) scale(2);">
    <img src="bride/animation/information-idle/information-idle1.png"
         style="visibility: hidden; opacity: 0; image-rendering: pixelated;">
    <img src="bride/animation/information-idle/information-idle2.png"
         style="visibility: visible; opacity: 1; image-rendering: pixelated;">
    <!-- position, left, top이 모두 제거됨 -->
</div>
```

---

## 4. 📋 변경 대상 애니메이션 상세

### 4.1 완료된 변경 목록

#### 🎭 Information-idle
```javascript
// 📍 위치: Line 488-500
this.addCharacter('information-idle', {
    isPngSequence: true,
    framePrefix: 'bride/animation/information-idle/information-idle',
    frameCount: 10,
    frameRate: 8,
    scale: 2,
    x: '0',
    y: '0',
    visible: false,
    loop: true,
    useVisibilityMethod: true  // 🎯 핵심 변경
});

// 📍 Show 함수: Line 2225-2244
showInformationIdle() {
    const informationIdleChar = this.characters.get('information-idle');
    if (informationIdleChar && informationIdleChar.element) {
        informationIdleChar.element.style.opacity = '1';
        informationIdleChar.element.style.top = '60%';    // 추가
        informationIdleChar.element.style.left = '50%';   // 추가
        informationIdleChar.isActive = true;
        if (informationIdleChar.isPngSequence) {
            this.startAnimation(informationIdleChar);
        }
    }
}
```

#### 🌸 Wreath
```javascript
// 📍 위치: Line 502-518
this.addCharacter('wreath', {
    isPngSequence: true,
    framePrefix: 'bride/animation/wreath/wreath',
    frameCount: 15,
    frameRate: 8,
    scale: 2,
    x: '50%',
    y: '20%',
    visible: false,
    loop: false,
    useVisibilityMethod: true,  // 🎯 핵심 변경
    onComplete: () => {
        this.onWreathAnimationComplete();
    }
});

// 📍 Show 함수: Line 3067-3076
startWreathAnimation() {
    const wreathChar = this.characters.get('wreath');
    if (wreathChar) {
        wreathChar.element.style.opacity = '1';
        wreathChar.element.style.left = '50%';  // calc() → 50%
        wreathChar.isActive = true;
        this.startAnimation(wreathChar);
    }
}
```

#### 🌿 Wreath-idle
```javascript
// 📍 위치: Line 520-533
this.addCharacter('wreath-idle', {
    isPngSequence: true,
    framePrefix: 'bride/animation/wreath-idle/wreath-idle',
    frameCount: 7,
    frameRate: 6,
    scale: 2,
    x: '50%',
    y: '20%',
    visible: false,
    loop: true,
    useVisibilityMethod: true  // 🎯 핵심 변경
});

// 📍 Show 함수: Line 3016-3024
onWreathAnimationComplete() {
    const wreathIdleChar = this.characters.get('wreath-idle');
    if (wreathIdleChar) {
        wreathIdleChar.element.style.opacity = '1';
        wreathIdleChar.element.style.left = '50%';  // calc() → 50%
        wreathIdleChar.isActive = true;
        this.startAnimation(wreathIdleChar);
    }
}
```

#### 🐰 Rabbit-hurt
```javascript
// 📍 위치: Line 535-548
this.addCharacter('rabbit-hurt', {
    isPngSequence: true,
    framePrefix: 'bride/animation/rabbit-hurt/rabbit-hurt',
    frameCount: 4,
    frameRate: 12,
    scale: 4,
    x: '56%',
    y: '60%',
    visible: false,
    loop: true,
    useVisibilityMethod: true  // 🎯 핵심 변경
});

// 📍 Show 함수: Line 3096-3112
switchRabbitToHurt() {
    const rabbitHurtChar = this.characters.get('rabbit-hurt');
    if (rabbitHurtChar && rabbitHurtChar.element) {
        rabbitHurtChar.element.style.display = 'block';
        rabbitHurtChar.element.style.visibility = 'visible';
        rabbitHurtChar.element.style.left = '50%';  // calc() → 50%
        rabbitHurtChar.element.style.top = rabbitHurtChar.y;
        rabbitHurtChar.element.style.position = 'absolute';
        rabbitHurtChar.element.style.transform = `translate(-50%, -50%) scale(${rabbitHurtChar.scale})`;
    }
}
```

### 4.2 변경되지 않은 애니메이션들 (참고용)

#### 🐰 Rabbit-idle (Src 방식 유지)
```javascript
// useVisibilityMethod: false (기본값)
// 이유: 이미 안정적으로 작동하고 있음
```

#### 🎵 기타 스프라이트시트 애니메이션들
```javascript
// hit-rabbit: isSpreadsheetBased: true
// 이유: 완전히 다른 시스템 (JSON 메타데이터 기반)
```

---

## 5. 🔧 핵심 함수별 상세 수정사항

### 5.1 addCharacter() 함수

#### 📍 위치: Line 1522-1610

#### 📝 변경 전후 비교
```javascript
// 🔍 핵심 분기 로직
if (character.useVisibilityMethod && character.isPngSequence) {
    // 🎯 Visibility 방식 처리
    character.frameImages = [];
    for (let i = 1; i <= character.frameCount; i++) {
        const img = document.createElement('img');
        const framePath = `${character.framePrefix}${i}.png`;
        img.src = framePath;

        // 🚨 이 부분이 핵심!
        img.style.cssText = `
            display: block;
            visibility: hidden;
            opacity: 0;
            image-rendering: pixelated;
            image-rendering: -moz-crisp-edges;
            image-rendering: crisp-edges;
            -webkit-user-drag: none;
            -webkit-user-select: none;
            user-select: none;
        `;
        // ❌ position: absolute; left: 0; top: 0; 제거됨!

        character.element.appendChild(img);
        character.frameImages.push(img);
    }
} else {
    // 🔧 기존 Src 방식 (변경 없음)
    const img = document.createElement('img');
    img.src = `${character.framePrefix}1.png`;
    img.style.cssText = `
        image-rendering: pixelated;
        image-rendering: -moz-crisp-edges;
        image-rendering: crisp-edges;
    `;
    character.element.appendChild(img);
    character.img = img;
}
```

#### ⚠️ 실수하기 쉬운 부분들
```javascript
// 🚨 자주 하는 실수들

// 1. 플래그 체크 순서
if (character.useVisibilityMethod) {  // ❌ isPngSequence 체크 누락
    // 스프라이트시트 애니메이션에도 적용될 수 있음
}

if (character.useVisibilityMethod && character.isPngSequence) {  // ✅ 올바름
    // PNG 시퀀스이면서 visibility 방식인 경우만
}

// 2. 스타일 설정 실수
img.style.position = 'absolute';  // ❌ 절대 하면 안 됨!
img.style.left = '0';            // ❌ 중앙정렬 깨짐
img.style.top = '0';             // ❌ 중앙정렬 깨짐

// 3. 배열 초기화 누락
// character.frameImages = [];  // ✅ 반드시 필요
```

### 5.2 updateFrame() 함수

#### 📍 위치: Line 1823-1855

#### 📝 상세 구현
```javascript
updateFrame(character) {
    if (!character.isPngSequence) {
        return;  // PNG 시퀀스가 아니면 처리하지 않음
    }

    if (character.useVisibilityMethod) {
        // 🎯 Visibility 방식 처리
        if (!character.frameImages || character.frameImages.length === 0) {
            console.error(`❌ frameImages not found for ${character.name}`);
            return;
        }

        // 🔄 모든 프레임 순회하며 visibility 설정
        character.frameImages.forEach((img, index) => {
            if (index === character.currentFrame) {
                // 현재 프레임: 보이게 설정
                img.style.visibility = 'visible';
                img.style.opacity = '1';
            } else {
                // 다른 프레임들: 숨기기
                img.style.visibility = 'hidden';
                img.style.opacity = '0';
            }
        });
    } else {
        // 🔧 Src 방식 처리 (기존)
        if (!character.img) {
            character.img = character.element.querySelector('img');
        }

        if (character.img) {
            const framePath = `${character.framePrefix}${character.currentFrame + 1}.png`;
            character.img.src = framePath;
        }
    }
}
```

#### 🐛 디버깅 팁
```javascript
// 🔍 문제 진단을 위한 디버깅 코드

function debugAnimationState(character) {
    console.log(`🔍 Animation Debug: ${character.name}`);
    console.log(`- useVisibilityMethod: ${character.useVisibilityMethod}`);
    console.log(`- isPngSequence: ${character.isPngSequence}`);
    console.log(`- currentFrame: ${character.currentFrame}`);

    if (character.useVisibilityMethod) {
        console.log(`- frameImages.length: ${character.frameImages.length}`);
        character.frameImages.forEach((img, index) => {
            console.log(`  Frame ${index}: visibility=${img.style.visibility}, opacity=${img.style.opacity}`);
        });
    } else {
        console.log(`- img.src: ${character.img?.src}`);
    }
}

// 사용법
updateFrame(character) {
    // 기존 로직...

    // 디버깅이 필요할 때만 주석 해제
    // debugAnimationState(character);
}
```

### 5.3 startAnimation() 함수

#### 📍 위치: Line 1693-1747

#### 📝 핵심 변경사항
```javascript
startAnimation(character) {
    if (character.animationTimeout) {
        clearTimeout(character.animationTimeout);
    }

    if (!character.isActive) return;

    // 🎯 방식에 관계없이 동일한 로직
    const animate = () => {
        if (!character.isActive) return;

        // 프레임 업데이트 (방식별 분기는 updateFrame에서 처리)
        this.updateFrame(character);

        // 다음 프레임 계산
        if (character.loop || character.currentFrame < character.frameCount - 1) {
            character.currentFrame = (character.currentFrame + 1) % character.frameCount;

            // 다음 프레임 스케줄링
            const frameDelay = 1000 / character.frameRate;
            character.animationTimeout = setTimeout(animate, frameDelay);
        } else {
            // 애니메이션 완료
            if (character.onComplete) {
                character.onComplete();
            }
        }
    };

    animate();
}
```

#### 💡 중요한 포인트
- `startAnimation`은 방식에 상관없이 동일하게 작동
- 실제 프레임 업데이트는 `updateFrame`에서 방식별로 분기
- 타이머 관리와 완료 콜백은 공통 로직

---

## 6. 🚨 실패 사례와 해결 방법

### 6.1 중앙정렬 관련 실패사례

#### ❌ 실패사례 1: 픽셀 계산 방식
```javascript
// 🚨 이렇게 하면 안 됨!
function calculateCenterOffset(imageName) {
    const sizes = {
        'information-idle': 400,
        'wreath': 144,
        'rabbit-hurt': 72
    };

    const halfWidth = sizes[imageName] / 2;
    return `calc(50% - ${halfWidth}px)`;
}

// ❌ 문제점들
// 1. 반응형 디자인에서 컨테이너 크기 변동
// 2. 스케일 요소 고려 안 됨
// 3. 이미지 내용의 실제 위치 고려 안 됨
// 4. 유지보수 어려움
```

**💡 교훈**: 픽셀 단위 보정보다는 CSS의 자동 중앙정렬 메커니즘을 활용하자.

#### ❌ 실패사례 2: 스타일 충돌 무시
```javascript
// 🚨 이런 스타일이 남아있으면 중앙정렬 깨짐
img.style.cssText = `
    position: absolute;  // 부모의 transform과 충돌
    left: 50%;          // 부모의 left: 50%와 중복
    top: 50%;           // 부모의 transform과 충돌
    transform: translate(-50%, -50%);  // 부모 transform과 중복
`;
```

**💡 해결책**: 부모-자식 간 역할 분담을 명확히 하자.
- 부모 div: 전체 위치와 중앙정렬 담당
- 자식 IMG: 오직 visibility와 픽셀화만 담당

#### ❌ 실패사례 3: 부분적 수정
```javascript
// 🚨 일부 함수만 수정하고 다른 함수는 놓침
showInformationIdle() {
    char.element.style.left = '50%';  // ✅ 수정함
}

// ❌ 이 함수는 수정 안 함
hideInformationIdle() {
    // 여전히 calc() 사용하거나 이전 방식 유지
}
```

**💡 해결책**: 관련된 모든 함수를 찾아서 일괄 수정하자.

### 6.2 애니메이션 동작 관련 실패사례

#### ❌ 실패사례 4: frameImages 배열 관리 실수
```javascript
// 🚨 배열 초기화를 까먹음
addCharacter(name, options) {
    const character = { /* ... */ };

    if (character.useVisibilityMethod) {
        // character.frameImages = [];  // ❌ 이걸 빼먹음

        for (let i = 1; i <= frameCount; i++) {
            // frameImages.push(img);  // undefined 배열에 push 시도
        }
    }
}
```

**💡 해결책**: 배열 초기화를 반드시 먼저 하자.

#### ❌ 실패사례 5: 프레임 인덱스 혼동
```javascript
// 🚨 0-based vs 1-based 인덱스 혼동
updateFrame(character) {
    const frameIndex = character.currentFrame;  // 0-based
    const framePath = `${prefix}${frameIndex}.png`;  // ❌ frame0.png가 됨
}

// ✅ 올바른 방식
updateFrame(character) {
    const frameIndex = character.currentFrame;  // 0-based
    const framePath = `${prefix}${frameIndex + 1}.png`;  // ✅ frame1.png
}
```

### 6.3 성능 관련 실패사례

#### ❌ 실패사례 6: 메모리 누수
```javascript
// 🚨 애니메이션 정리를 안 함
hideCharacter(character) {
    character.element.style.opacity = '0';
    // ❌ 타이머 정리 안 함
    // ❌ frameImages 배열 정리 안 함
}

// ✅ 올바른 정리
hideCharacter(character) {
    character.element.style.opacity = '0';
    character.isActive = false;

    if (character.animationTimeout) {
        clearTimeout(character.animationTimeout);  // 타이머 정리
        character.animationTimeout = null;
    }

    // frameImages는 DOM에서 제거하지 않음 (재사용 위해)
    // 하지만 visibility는 hidden으로 설정
    if (character.frameImages) {
        character.frameImages.forEach(img => {
            img.style.visibility = 'hidden';
            img.style.opacity = '0';
        });
    }
}
```

---

## 7. 🎯 성공 사례와 베스트 프랙티스

### 7.1 ✅ 성공사례: 단계적 적용

#### 🎯 전략
기존 시스템을 건드리지 않고 새 시스템을 단계적으로 도입

```javascript
// Phase 1: 플래그 시스템 구축
const character = {
    useVisibilityMethod: false,  // 기본값은 기존 방식
    // 기존 속성들 유지...
};

// Phase 2: 선택적 적용
this.addCharacter('test-animation', {
    useVisibilityMethod: true,  // 테스트용만 새 방식
    // 나머지는 기존 방식 유지
});

// Phase 3: 안정화 후 확산
// 테스트 완료 후 다른 애니메이션들에도 적용
```

**💡 교훈**: 한 번에 모든 걸 바꾸지 말고 점진적으로 적용하자.

### 7.2 ✅ 성공사례: 역할 분담 명확화

#### 🎯 부모-자식 역할 분담
```javascript
// ✅ 부모 div: 위치와 변형 담당
.pixel-character {
    position: absolute;
    left: 50%;                    // 가로 위치
    top: 60%;                     // 세로 위치
    transform: translate(-50%, -50%) scale(2);  // 중앙정렬 + 스케일
}

// ✅ 자식 IMG: 표시/숨김만 담당
img {
    visibility: hidden;           // 표시 상태만
    opacity: 0;                   // 투명도만
    image-rendering: pixelated;   // 렌더링 방식만
    // 위치 관련 스타일 없음!
}
```

### 7.3 ✅ 성공사례: 공통 인터페이스

#### 🎯 방식에 무관한 통일된 API
```javascript
// ✅ 어떤 방식이든 동일하게 사용
const character = this.characters.get('any-animation');
this.startAnimation(character);   // 방식 무관
this.stopAnimation(character);    // 방식 무관
this.updateFrame(character);      // 내부에서 분기 처리

// 사용자는 useVisibilityMethod만 설정하면 됨
```

**💡 교훈**: 내부 구현은 복잡해도 외부 인터페이스는 단순하게 유지하자.

---

## 8. 🔍 트러블슈팅 가이드

### 8.1 문제 진단 체크리스트

#### 🎯 중앙정렬 문제
```javascript
// ✅ 체크리스트
function diagnoseCenteringIssue(character) {
    console.log('🔍 중앙정렬 진단 시작...');

    // 1. 부모 div 스타일 확인
    const parentStyle = character.element.style;
    console.log(`부모 left: ${parentStyle.left}`);        // '50%'이어야 함
    console.log(`부모 transform: ${parentStyle.transform}`); // translate(-50%, -50%)

    // 2. 자식 IMG 스타일 확인 (visibility 방식)
    if (character.useVisibilityMethod && character.frameImages) {
        character.frameImages.forEach((img, index) => {
            if (img.style.position === 'absolute') {
                console.error(`❌ Frame ${index}에 position: absolute 발견!`);
            }
            if (img.style.left || img.style.top) {
                console.error(`❌ Frame ${index}에 left/top 스타일 발견!`);
            }
        });
    }

    // 3. DOM 구조 확인
    const imgCount = character.element.querySelectorAll('img').length;
    console.log(`IMG 요소 개수: ${imgCount}`);

    if (character.useVisibilityMethod) {
        if (imgCount !== character.frameCount) {
            console.error(`❌ 예상 프레임 수: ${character.frameCount}, 실제: ${imgCount}`);
        }
    } else {
        if (imgCount !== 1) {
            console.error(`❌ Src 방식인데 IMG가 ${imgCount}개`);
        }
    }
}
```

#### 🎯 애니메이션 동작 문제
```javascript
function diagnoseAnimationIssue(character) {
    console.log('🔍 애니메이션 진단 시작...');

    // 1. 기본 상태 확인
    console.log(`isActive: ${character.isActive}`);
    console.log(`currentFrame: ${character.currentFrame}`);
    console.log(`frameCount: ${character.frameCount}`);
    console.log(`useVisibilityMethod: ${character.useVisibilityMethod}`);

    // 2. 방식별 확인
    if (character.useVisibilityMethod) {
        if (!character.frameImages || character.frameImages.length === 0) {
            console.error('❌ frameImages 배열이 비어있음');
            return;
        }

        // 현재 표시되는 프레임 확인
        let visibleCount = 0;
        character.frameImages.forEach((img, index) => {
            if (img.style.visibility === 'visible' && img.style.opacity === '1') {
                console.log(`✅ Frame ${index} 표시 중`);
                visibleCount++;
            }
        });

        if (visibleCount !== 1) {
            console.error(`❌ 표시 중인 프레임 수: ${visibleCount} (1개여야 함)`);
        }
    } else {
        if (!character.img) {
            console.error('❌ img 요소가 없음');
            return;
        }
        console.log(`현재 src: ${character.img.src}`);
    }

    // 3. 타이머 상태 확인
    if (character.animationTimeout) {
        console.log('✅ 애니메이션 타이머 활성');
    } else {
        console.log('⚠️ 애니메이션 타이머 없음');
    }
}
```

### 8.2 자주 발생하는 문제들

#### 🚨 문제 1: "애니메이션이 시작되지 않음"
```javascript
// 🔍 가능한 원인들
// 1. isActive가 false
character.isActive = true;  // 확인

// 2. frameImages가 비어있음 (visibility 방식)
if (character.frameImages.length === 0) {
    // addCharacter에서 IMG 생성이 안 됨
}

// 3. img 요소가 없음 (src 방식)
if (!character.img) {
    character.img = character.element.querySelector('img');
}

// 4. 타이머가 이미 실행 중
if (character.animationTimeout) {
    clearTimeout(character.animationTimeout);
}
```

#### 🚨 문제 2: "이미지가 화면에 나타나지 않음"
```javascript
// 🔍 가능한 원인들
// 1. 부모 div가 숨겨져 있음
character.element.style.opacity = '1';
character.element.style.display = 'block';
character.element.style.visibility = 'visible';

// 2. 현재 프레임 IMG가 숨겨져 있음 (visibility 방식)
if (character.frameImages[character.currentFrame]) {
    const img = character.frameImages[character.currentFrame];
    img.style.visibility = 'visible';
    img.style.opacity = '1';
}

// 3. 이미지 로딩 실패
character.frameImages?.forEach((img, index) => {
    if (img.complete && img.naturalWidth === 0) {
        console.error(`Frame ${index} 로딩 실패: ${img.src}`);
    }
});
```

#### 🚨 문제 3: "중앙정렬이 안 맞음"
```javascript
// 🔍 체크 포인트들
// 1. 자식 IMG에 position 스타일이 있는가?
character.frameImages?.forEach(img => {
    if (img.style.position) {
        img.style.position = '';  // 제거
    }
    if (img.style.left) {
        img.style.left = '';      // 제거
    }
    if (img.style.top) {
        img.style.top = '';       // 제거
    }
});

// 2. 부모 div에 올바른 위치가 설정되어 있는가?
character.element.style.left = '50%';
character.element.style.top = '60%';  // 또는 원하는 위치

// 3. transform이 올바르게 설정되어 있는가?
const transform = character.element.style.transform;
if (!transform.includes('translate(-50%, -50%)')) {
    console.error('❌ translate(-50%, -50%) 없음');
}
```

---

## 9. 📈 성능 최적화 팁

### 9.1 메모리 관리

#### 🎯 선택적 로딩
```javascript
// ✅ 필요할 때만 프레임 생성
async function createFramesOnDemand(character) {
    if (character.frameImages.length > 0) {
        return;  // 이미 생성됨
    }

    for (let i = 1; i <= character.frameCount; i++) {
        const img = document.createElement('img');
        img.src = `${character.framePrefix}${i}.png`;

        // 이미지 로딩 대기
        await new Promise(resolve => {
            img.onload = resolve;
            img.onerror = resolve;  // 오류시에도 진행
        });

        character.frameImages.push(img);
        character.element.appendChild(img);
    }
}
```

#### 🎯 메모리 정리
```javascript
function cleanupCharacter(character) {
    // 타이머 정리
    if (character.animationTimeout) {
        clearTimeout(character.animationTimeout);
        character.animationTimeout = null;
    }

    // 이벤트 리스너 정리
    if (character.frameImages) {
        character.frameImages.forEach(img => {
            img.onload = null;
            img.onerror = null;
        });
    }

    // DOM에서는 제거하지 않음 (재사용을 위해)
    // 하지만 비활성화
    character.isActive = false;
}
```

### 9.2 렌더링 최적화

#### 🎯 batch 업데이트
```javascript
// ✅ DOM 변경을 한 번에 처리
function updateFrameOptimized(character) {
    // requestAnimationFrame으로 렌더링 최적화
    requestAnimationFrame(() => {
        if (character.useVisibilityMethod) {
            // 모든 변경을 한 번에 적용
            const fragment = document.createDocumentFragment();

            character.frameImages.forEach((img, index) => {
                const shouldShow = (index === character.currentFrame);
                img.style.visibility = shouldShow ? 'visible' : 'hidden';
                img.style.opacity = shouldShow ? '1' : '0';
            });
        }
    });
}
```

---

## 10. 🔮 향후 확장 가이드

### 10.1 새 애니메이션 추가하기

#### 📝 Step-by-Step 가이드
```javascript
// Step 1: 애니메이션 정의 (constructor에서)
this.addCharacter('새-애니메이션', {
    isPngSequence: true,
    framePrefix: 'bride/animation/새-애니메이션/frame',
    frameCount: 8,                    // 프레임 수에 맞게
    frameRate: 10,                    // FPS 설정
    scale: 2,                         // 크기
    x: '50%',                         // 기본 x 위치
    y: '50%',                         // 기본 y 위치
    visible: false,                   // 초기엔 숨김
    loop: true,                       // 반복 여부
    useVisibilityMethod: true         // 🎯 새 방식 사용
});

// Step 2: Show 함수 구현
show새애니메이션() {
    const char = this.characters.get('새-애니메이션');
    if (char && char.element) {
        // 기본 표시 설정
        char.element.style.opacity = '1';
        char.element.style.left = '50%';     // 중앙정렬
        char.element.style.top = '60%';      // 원하는 위치
        char.isActive = true;

        // 애니메이션 시작
        this.startAnimation(char);

        // 필요시 완료 콜백
        if (char.onComplete) {
            char.onComplete = () => {
                console.log('새-애니메이션 완료!');
                this.hide새애니메이션();
            };
        }
    }
}

// Step 3: Hide 함수 구현
hide새애니메이션() {
    const char = this.characters.get('새-애니메이션');
    if (char && char.element) {
        char.element.style.opacity = '0';
        char.isActive = false;
        this.stopAnimation(char);
    }
}

// Step 4: 호출 위치 추가 (적절한 섹션에서)
// 예: Section 변경 시, 스크롤 이벤트 등에서 호출
```

### 10.2 기존 애니메이션 방식 변경하기

#### 📝 Src → Visibility 변경 가이드
```javascript
// Step 1: 기존 설정 확인
const 기존설정 = {
    isPngSequence: true,
    framePrefix: 'groom/animation/existing/frame',
    frameCount: 6,
    // useVisibilityMethod: false  // 기본값
};

// Step 2: 플래그만 추가
const 변경설정 = {
    ...기존설정,
    useVisibilityMethod: true  // 🎯 이것만 추가!
};

// Step 3: 관련 함수들 위치 확인
// - show 함수에서 left/top 설정 확인
// - update 함수에서 left/top 설정 확인
// - 기타 positioning 관련 코드 확인

// Step 4: 테스트
// - 애니메이션 실행 확인
// - 중앙정렬 확인
// - 성능 확인
```

### 10.3 하이브리드 시스템 구축

#### 🎯 상황에 따른 자동 선택
```javascript
function getOptimalAnimationMethod(frameCount, memoryUsage, quality) {
    // 프레임 수가 적고 품질이 중요한 경우
    if (frameCount <= 10 && quality === 'high') {
        return true;  // visibility 방식
    }

    // 프레임 수가 많고 메모리가 제한적인 경우
    if (frameCount > 15 || memoryUsage === 'low') {
        return false;  // src 방식
    }

    // 기본값
    return true;  // visibility 방식 선호
}

// 사용 예시
this.addCharacter('adaptive-animation', {
    isPngSequence: true,
    framePrefix: 'animation/frames/frame',
    frameCount: 12,
    useVisibilityMethod: getOptimalAnimationMethod(12, 'normal', 'high')
});
```

---

## 11. 📚 참고 자료

### 11.1 관련 파일들
- `simplePixelCharacter.js`: 메인 애니메이션 로직
- `main.css`: 스타일 정의
- 애니메이션 이미지들: `/bride/animation/`, `/groom/animation/`

### 11.2 중요한 CSS 속성들
```css
/* 중앙정렬을 위한 핵심 CSS */
.pixel-character {
    position: absolute;
    left: 50%;                           /* 부모 기준 50% 위치 */
    top: 60%;                            /* 부모 기준 60% 위치 */
    transform: translate(-50%, -50%);    /* 자신 기준 -50% 이동 = 중앙정렬 */
}

/* 픽셀 아트를 위한 렌더링 설정 */
img {
    image-rendering: pixelated;          /* 픽셀 아트 스타일 유지 */
    image-rendering: -moz-crisp-edges;   /* Firefox 지원 */
    image-rendering: crisp-edges;        /* 기타 브라우저 */
}
```

### 11.3 브라우저 호환성
- **Modern browsers**: 모든 기능 지원
- **IE 11**: `image-rendering: pixelated` 미지원
- **Safari**: `image-rendering: crisp-edges` 권장

---

## 12. 🏁 마무리

이 가이드를 통해 다음과 같은 내용을 학습했습니다:

### ✅ 핵심 성과
1. **두 애니메이션 방식의 통합**: Src와 Visibility 방식을 하나의 시스템으로 통합
2. **완벽한 중앙정렬**: CSS Transform의 동작 원리를 이해하고 정확한 중앙정렬 달성
3. **단계적 적용**: 기존 시스템을 건드리지 않고 점진적으로 새 시스템 도입
4. **문제 해결 방법론**: 픽셀 계산보다는 근본 원인을 제거하는 접근법

### 💡 주요 교훈
- **근본 원인 파악**: 표면적 증상보다 근본 원인을 찾아 해결
- **점진적 변경**: 한 번에 모든 걸 바꾸지 말고 단계적으로 적용
- **역할 분담**: 부모-자식 요소 간 명확한 역할 분담
- **일관성**: 외부 인터페이스의 일관성 유지

### 🔧 실무 팁
- 새로운 애니메이션은 `useVisibilityMethod: true` 사용 권장
- 중앙정렬 문제 시 자식 요소의 `position: absolute` 확인
- 디버깅 시 제공된 진단 함수들 활용
- 성능 고려사항을 염두에 두고 방식 선택

이제 이 가이드를 바탕으로 안정적이고 확장 가능한 애니메이션 시스템을 구축할 수 있습니다! 🎉

## 2. 통합 시스템 구현

### 2.1 useVisibilityMethod 플래그 추가
```javascript
// addCharacter 호출 시 방식 선택
this.addCharacter('animation-name', {
    isPngSequence: true,
    framePrefix: 'path/to/frames/frame',
    frameCount: 10,
    useVisibilityMethod: true  // true: visibility 방식, false: src 방식
});
```

### 2.2 addCharacter 함수 수정
```javascript
if (character.useVisibilityMethod && character.isPngSequence) {
    // Visibility 방식: 모든 프레임을 개별 IMG로 생성
    character.frameImages = [];
    for (let i = 1; i <= character.frameCount; i++) {
        const img = document.createElement('img');
        img.src = `${character.framePrefix}${i}.png`;
        img.style.cssText = `
            display: block;
            visibility: hidden;
            opacity: 0;
            image-rendering: pixelated;
        `;
        character.element.appendChild(img);
        character.frameImages.push(img);
    }
} else {
    // Src 방식: 단일 IMG 생성
    const img = document.createElement('img');
    img.src = `${character.framePrefix}1.png`;
    character.element.appendChild(img);
    character.img = img;
}
```

## 3. 중앙정렬 문제 해결

### 3.1 문제 원인
```html
<!-- 문제가 있던 구조 -->
<div class="pixel-character" style="left: 50%; transform: translate(-50%, -50%);">
    <img style="position: absolute; left: 0; top: 0;"> <!-- 이 부분이 문제 -->
</div>
```

**문제점**: IMG 요소의 `position: absolute; left: 0; top: 0;`이 부모 div의 `transform: translate(-50%, -50%)` centering과 충돌

### 3.2 해결 방법

#### Step 1: IMG 내부 positioning 제거
```javascript
// 기존 (문제 있던 코드)
img.style.cssText = `
    position: absolute;  // 제거
    left: 0;            // 제거
    top: 0;             // 제거
    visibility: hidden;
    // 기타 스타일...
`;

// 수정 후
img.style.cssText = `
    visibility: hidden;
    opacity: 0;
    image-rendering: pixelated;
    // positioning 관련 스타일 완전 제거
`;
```

#### Step 2: 부모 div에 명시적 positioning 설정
```javascript
// 각 애니메이션 show 함수에서 설정
showInformationIdle() {
    const char = this.characters.get('information-idle');
    char.element.style.top = '60%';
    char.element.style.left = '50%';  // 명시적 설정
    char.element.style.opacity = '1';
}
```

## 4. 변경된 애니메이션 목록

### 4.1 Visibility 방식으로 변경된 애니메이션들
```javascript
// 모든 애니메이션에 useVisibilityMethod: true 적용
const visibilityAnimations = [
    'wreath',           // 화환 애니메이션
    'wreath-idle',      // 화환 대기 애니메이션
    'information-idle', // 정보 대기 애니메이션
    'rabbit-hurt',      // 토끼 다침 애니메이션
    'hit-rabbit'        // 토끼 타격 애니메이션
];
```

### 4.2 각 애니메이션별 설정값
```javascript
// Information-idle
this.addCharacter('information-idle', {
    framePrefix: 'bride/animation/information-idle/information-idle',
    frameCount: 10,
    scale: 2,
    useVisibilityMethod: true
});

// Wreath
this.addCharacter('wreath', {
    framePrefix: 'bride/animation/wreath/wreath',
    frameCount: 15,
    scale: 2,
    useVisibilityMethod: true
});
```

## 5. 주요 함수별 수정사항

### 5.1 updateFrame() 함수
```javascript
updateFrame(character) {
    if (character.useVisibilityMethod) {
        // Visibility 방식 처리
        character.frameImages.forEach((img, index) => {
            if (index === character.currentFrame) {
                img.style.visibility = 'visible';
                img.style.opacity = '1';
            } else {
                img.style.visibility = 'hidden';
                img.style.opacity = '0';
            }
        });
    } else {
        // Src 방식 처리 (기존)
        const framePath = `${character.framePrefix}${character.currentFrame + 1}.png`;
        character.img.src = framePath;
    }
}
```

### 5.2 각 애니메이션 show 함수들
```javascript
// 이전: 픽셀 계산 기반 보정
rabbitChar.element.style.left = 'calc(50% - 72px)';

// 변경 후: 단순 50% + transform 의존
rabbitChar.element.style.left = '50%';
```

## 6. 트러블슈팅

### 6.1 중앙정렬이 안 맞을 때
1. IMG 요소에 `position: absolute` 스타일이 남아있는지 확인
2. 부모 div에 `left: 50%` 설정이 되어있는지 확인
3. `transform: translate(-50%, -50%)` 스타일이 적용되어있는지 확인

### 6.2 애니메이션이 작동하지 않을 때
1. `useVisibilityMethod` 플래그가 올바르게 설정되었는지 확인
2. `frameImages` 배열이 제대로 생성되었는지 확인
3. `updateFrame` 함수에서 올바른 분기로 진입하는지 확인

## 7. 성능 고려사항

### 7.1 메모리 사용량
- **Src 방식**: IMG 1개 × 애니메이션 수
- **Visibility 방식**: IMG (프레임 수) × 애니메이션 수

### 7.2 권장사항
- 프레임 수가 적은 애니메이션: Visibility 방식 권장
- 프레임 수가 많은 애니메이션: Src 방식 고려
- 부드러운 전환이 중요한 경우: Visibility 방식 필수

## 8. 향후 확장 방법

### 8.1 새 애니메이션 추가 시
```javascript
// 1. 애니메이션 정의
this.addCharacter('new-animation', {
    isPngSequence: true,
    framePrefix: 'path/to/new-animation/frame',
    frameCount: 8,
    useVisibilityMethod: true,  // 방식 선택
    scale: 2,
    x: '50%',
    y: '60%'
});

// 2. Show 함수 구현
showNewAnimation() {
    const char = this.characters.get('new-animation');
    char.element.style.opacity = '1';
    char.element.style.left = '50%';  // 중앙정렬
    char.element.style.top = '60%';   // 위치 설정
    this.startAnimation(char);
}
```

### 8.2 기존 애니메이션 방식 변경 시
```javascript
// 1. useVisibilityMethod 플래그만 추가/제거
// 2. show 함수에서 positioning 조정
// 3. 테스트 후 적용
```

---

**참고**: 이 가이드는 `/Users/munjiwon/me/wedding/bride/src/scripts/simplePixelCharacter.js` 파일 기준으로 작성되었습니다.